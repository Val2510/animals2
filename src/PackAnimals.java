import java.time.LocalDate;
import java.util.ArrayList;

class PackAnimals extends Animal {
    public PackAnimals(String name, String type, LocalDate birthDate, ArrayList<String> commands) {
        super(name, type, birthDate, commands);
    }
}
