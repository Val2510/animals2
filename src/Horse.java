import java.time.LocalDate;
import java.util.ArrayList;

class Horse extends PackAnimals {
    public Horse(String name, String type, LocalDate birthDate, ArrayList<String> commands) {
        super(name, type, birthDate, commands);
    }
}
