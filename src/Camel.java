import java.time.LocalDate;
import java.util.ArrayList;

class Camel extends PackAnimals {
    public Camel(String name, String type, LocalDate birthDate, ArrayList<String> commands) {
        super(name, type, birthDate, commands);
    }
}
